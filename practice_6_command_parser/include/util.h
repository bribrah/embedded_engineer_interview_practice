#pragma once
#include <vector>
#include <string>
#include <sstream>
#include <cctype>
#include <iostream>

std::vector<std::string> splitString(std::string s, char delim);
std::string toLower(std::string str);
